git config --list to check current username & email in your local repo

git config user.name "Ram XYZ"
git config user.email "ram@xyz.com"
git config credential.username  "Ram XYZ"
git config credential.username --global "Ram Maddela"
